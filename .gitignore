node_modules/
dist/
.astro/
playwright-report/
test-results/
.DS_Store
__pycache__/

# Screen evidence. Regenerated wholesale by scripts/screenshots.mjs whenever
# anything visual changes, so tracking it would add ~15MB of churn per commit.
# What makes the verification real is that the screens were looked at, and that
# is recorded in docs/progress.md.
docs/screenshots/

# The design brief. Kept in Korean, and kept out of the repository:
# it is the author's own requirements document, not a project file.
# Everything else in docs/ is written in English.
docs/plan.md
