# Progress log

- Updated: 2026-09-16
- Scope document: `docs/current-task.md`
- Coverage map: `docs/papers/attention-is-all-you-need-coverage.md`
- Status: **P0 + P1 → design rebuild → single-page rebuild and full rewrite of the
  prose → the two rails rearranged → one content width → **the remaining six
  sections written (P2, P3, P4)**. All twelve sections are now written.

> The language rule for this repository: internal documents and code comments are
> written in English. Only the Korean wiki page itself is in Korean.
> `docs/plan.md` is the author's own design brief, stays in Korean, and is listed
> in `.gitignore`.
>
> `docs/screenshots/` is not tracked either. It is regenerated by
> `node scripts/screenshots.mjs docs/screenshots`, and the verification tables
> below record what was seen in it rather than the images themselves.

## 1. How to run it

```bash
npm install                        # dependencies (once)
npx playwright install chromium    # for the browser checks (once)

npm run dev            # dev server → http://localhost:4321/llm-visual-lab/
npm run build          # static build → dist/
npm run preview        # serves the build under /llm-visual-lab/

npm test               # checks on the maths (vitest, 147)
npm run fixtures       # regenerate the independent references with numpy, Fraction, Decimal
npm run typecheck      # tsc --noEmit
npm run test:e2e       # browser checks (playwright, 56 desktop + 56 mobile)

# These two need the preview server already running
node scripts/screenshots.mjs docs/screenshots        # screen evidence: pages × theme × width
node scripts/shoot-element.mjs <out.png> <URL> <selector> [click::<selector>] [wait::<ms>]
                                                    # one component, optionally after interacting
```

There is no `lint` or `format check` script. Point 1 of Done in AGENTS.md says to
run only the commands the project provides, so no check is invented in order to
record it as passing. `typecheck` stands in its place.

## 2. What was built

### Documents

Originally 8 pages per language. They were merged into one page in the
single-page rebuild, and that page became the home page; see the last two
sections of this log for the current routes.

### The calculations and the labs

- `src/lib/math/` — pure functions, nothing about the UI. `matrix.ts`,
  `softmax.ts`, `attention.ts`, `errors.ts`, `format.ts`, `examples.ts`
- `src/components/lab/` — `MatrixGrid`, `EditableMatrix`, `TermExpansion`,
  `MatrixProductExplorer`, `DotProductExplorer`, `SoftmaxExplorer`, `AttentionLab`
- Every number on screen comes from one call to `computeAttention()`. The
  diagrams, the equations and the tables all read the same result.
- The worked example: 3 tokens, d_model=4, d_k=2, d_v=2, one head. The values are
  picked so every entry of QKᵀ comes out an integer.
- The attention lab steps manually through seven stages (input → projection →
  scores → divide → mask → probabilities → weighted sum). Nothing plays
  automatically.

### Diagrams

Two to begin with, five more added later (see the last section):

- `TransformerMap.astro` — figure 1 of the paper, redrawn as our own SVG. Box
  coordinates are computed bottom-up, so no arrow ends inside a box. Only the
  attention block explained at this stage is emphasised.
- `PathLengthDiagram.astro` — the Maximum Path Length comparison from Table 1
- Both use the theme tokens, and both carry a full description in `<desc>` for
  readers who cannot see the figure.

## 3. Technical choices, and where they diverge from the plan

| | Version |
| --- | --- |
| Astro | 7.3.2 (static output, built-in i18n) |
| @astrojs/mdx | 8.0.1 |
| @astrojs/markdown-remark | 7.3.x |
| React | 19.3.0 |
| KaTeX | 0.18.7 + remark-math 6 + rehype-katex 7 |
| Vitest | 5.0.0 / Playwright 1.63.0 / TypeScript 5.9.3 |

Where this diverges from section 10 of `docs/plan.md`, and why:

1. **`src/content/docs/{ko,en}/`** — the plan said `src/content/{ko,en}/`. One
   extra level lets a single Astro collection hold both languages and split them
   on the first part of the entry id. The Korean and English versions of a
   document must agree on everything after the language code, and a browser check
   enforces that.
2. **`src/components/lab/`** — the plan's `src/labs/` is unused. Every lab in this
   scope is embedded in the prose; none of them needs a page of its own.
3. **The `markdown.processor` setting** — Astro 7 changed its default markdown
   processor. To use remark and rehype plugins you have to install
   `@astrojs/markdown-remark` separately and pass them through
   `markdown: { processor: unified({...}) }`. Passing them as
   `markdown.remarkPlugins` leaves MDX without them, and the braces inside
   equations get parsed as JSX.
4. **Helpers for `getStaticPaths` have to be imported from a module.** Defined
   inside a page's frontmatter they are invisible when it runs, and it fails with
   `parseId is not defined`. `parseEntryId` lives in `lib/i18n/routing.ts`.

## 4. Verification

Measured on macOS (Darwin 25.6.0), Apple M4 Pro, Node v26.0.0, Chromium
(Playwright 1.63.0). The browser checks run against **the build, served under
`/llm-visual-lab/`**, not against the dev server.

### The maths

| Check | Result |
| --- | --- |
| Against independent references (tolerance 1e-9) | **pass** — 99 vitest cases at the time. The references are computed separately by `tests/fixtures/generate_fixtures.py` with numpy einsum, `Fraction` and 50-digit `Decimal`. The TypeScript uses a triple loop and a max-subtracting softmax, so the two routes are genuinely different. |
| Equal scores | **pass** — row 3 of the worked example is (2,2,2), so the probabilities are exactly one third each |
| Unequal scores with a non-zero V | **pass** — row 1 (0,4,2) → (0.045, 0.768, 0.187), output (0.323, 2.490) |
| Causal mask | **pass** — row 1 gives (1,0,0) and its output matches row 1 of V, (3,0), exactly |
| Division on and off | **pass** — off gives (0.016, 0.867, 0.117) |
| A fully blocked row | **pass** — refused with `MaskError`. It does not quietly return NaN |
| Stability at large scores | **pass** — (1000, 1001, 999) does not overflow. The reference is computed in Decimal |
| Mismatched shapes | **pass** — a `ShapeError` that says what is wrong |
| Which maximum the stabilisation uses | **pass** — the largest **allowed** score, blocked positions excluded |
| The numbers quoted in the prose | **pass** — a test pins every value written out in the text, so the prose cannot drift from the calculation |

Row sums of 1 are not treated as proof. Every intermediate matrix (Q, K, V, Kᵀ,
S, scaled, mask, A, O) is compared.

### Browser (92 passing at the time: 46 desktop at 1440px + 46 mobile at 390px)

| Check | Result |
| --- | --- |
| All 16 pages reachable by direct URL | **pass** — all 200, with zero 4xx responses, console errors or page errors |
| Redirect from the root `/llm-visual-lab/` | **pass** — goes to `/llm-visual-lab/ko/` |
| Asset loading | **pass** — no 404 for CSS, JS or the KaTeX fonts. `document.fonts.check('16px KaTeX_Math')` is true |
| Internal links | **pass** — every link collected returns 200 and contains `/llm-visual-lab/` |
| Equation rendering | **pass** — `.katex-html` is drawn and no `$$` shows in the prose |
| Language switch | **pass** — lands on the other language version of the same document, and back again |
| Korean and English agree numerically | **pass** — the weight matrices are identical in both |
| Stepping | **pass** — seven steps, forwards and back; the next button disables at the end |
| Selecting a cell | **pass** — selecting S(1,2) gives products [4, 0] summing to 4, and updates on another cell |
| Editing an input | **pass** — X(1,1) from 1 to 3 takes S(1,2) from 4 to 8 |
| Reset | **pass** — values, step and toggles all return to the start |
| Toggling the mask | **pass** — row 1 becomes (1.000, −∞, −∞) and the output (3.000, 0.000) |
| Toggling the division | **pass** |
| Arrow keys | **pass** — ←↑↓→ and Home/End move between matrix cells |
| Skip-to-content link | **pass** — focused on the first Tab, follows on Enter |
| Roving tabindex | **pass** — exactly one cell in a matrix has tabindex=0 |
| Sideways scroll (1440 / 768 / 390) | **pass** — the document never exceeds the viewport width |
| 200% zoom (equivalent to 720px) | **pass** — no sideways scroll, and the prose is not clipped |
| Reduced motion | **pass** — `--motion` is 0 |
| Reading without JavaScript | **pass** — the prose, the equations, the default numbers in the labs and the guidance are all in the static HTML |
| Narrow layout | **pass** — the article comes before the contents, and the masthead tools are not clipped |
| Wide layout | **pass** — the contents rail is placed correctly and the masthead link to it is hidden |
| Update time after an interaction | **pass** — at most **25.7ms** to two frames after selecting a cell (the limit is 100ms). M4 Pro, Chromium, the 3-token example |

**All four** labs are checked. What each one was checked on:

| Lab | What was exercised |
| --- | --- |
| `AttentionLab` (attention from the paper) | seven steps, cell selection, editing X, the mask toggle, the division toggle, reset, arrow keys, roving tabindex |
| `MatrixProductExplorer` (matrix product) | cell selection and the row/column highlight on the editable matrix, editing W_Q, reset |
| `DotProductExplorer` (dot product) | four coordinate changes (perpendicular → 0, doubled length → 8, reversed → −4), cos θ following along, reset, overlapping arrows staying distinct |
| `SoftmaxExplorer` (softmax) | raising one score (do the others fall?), shifting all of them (do the probabilities hold?), reset |

### Reviewing the prose

| Check | Result |
| --- | --- |
| Wording forbidden by section 7 of the plan | **pass** — swept for 당연, 그냥, 쉽죠, 물론 and for "obviously", "simply", "of course" and rewrote them. One was kept: "그냥 나누면 안 될까요" in the softmax section points at a **naive method**, not at the reader's difficulty. |
| Korean and English structurally parallel | **pass** — across all eight pairs, the number and kind of Notes, the number of Expands, the count of `##` headings, equation blocks and tables, and the status / updated / notYet entries in the frontmatter all match. |
| The same numbers quoted in both | **pass** — 0.045 / 0.768 / 0.187 / 0.016 / 0.867 / 0.117 / 0.944 / 0.056 / 1.333 / 0.323 / 2.490 / 54.598 / 62.987 / 28.4 / 41.8 / 41.0 and the rest appear in both or in neither. |
| The prose agrees with the calculation | **pass** — a unit test pins every quoted value. Changing the prose breaks the test. |

### Screen evidence

`node scripts/screenshots.mjs <directory>` saved 8 pages × desktop and mobile ×
light and dark = 32 images. What they confirmed:

- In both themes the prose, the equations, the matrix heatmap and the blocked
  cells are readable.
- On desktop the attention lab breaks out of the prose measure (624px) to 856px
  and all seven steps are visible.
- On mobile the matrices stack vertically and the term table is not clipped.
- In the architecture diagram no arrow ends inside a box and no label is clipped,
  in either language.

### Fixed during review, then re-checked

1. The lab was trapped at the prose measure and the seventh step was cut off →
   `.prose` became a two-column grid so labs and figures can break out. (Astro's
   `<astro-island>` is `display: contents`, so the real grid item is the
   component root.)
2. The prose table styles (`.prose th/td`) were reaching the matrices inside the
   labs and wrecking the cells → narrowed to `.prose > table`.
3. In the architecture diagram the "Decoder" title overlapped the Linear box and
   arrows ended inside boxes → rewritten to compute coordinates bottom-up rather
   than writing each one by hand.
4. The English "Positional Encoding" label was clipped by the viewBox → viewBox
   widened.
5. The positional-encoding arrow passed through the ⊕ circle → sign error at the
   endpoint, fixed.
6. On mobile you had to scroll past thirteen lines of contents to reach the prose
   → on narrow screens the article comes first, with a link to the contents added
   to the masthead.
7. At 390px the theme buttons in the masthead were clipped off-screen and could
   not be pressed → spacing and type adjusted for narrow widths, plus a check
   that catches clipping.
8. The label `√d_k 로 나누기` became `√D_K` through a CSS uppercase transform →
   transform removed, subscript characters used.
9. The coverage table was set in a monospaced face, which spaces Korean out to
   full width → changed to the body face.
10. On mobile equation (1) overflowed by 8% and had to be pushed sideways → 0.92em
    for equations on narrow screens.
11. **The matrix-product lab drew its input matrices twice.** The upper pair was
    editable, the lower pair a copy for highlighting, and selecting a cell lit up
    only the copy. The prose said "press a cell and the row and column it came
    from are coloured", but a different matrix was lighting up. → `EditableMatrix`
    now takes the highlight itself and the copy is gone. The lab also halved in
    height.
12. **In the dot-product lab the two default arrows coincided exactly.** Both q
    and k were (2, 0), so they looked like one arrow with two labels on top of
    each other. → the lower is now thick and pale, the upper thin and dark, and
    the labels are pushed in opposite directions. The example in the prose is
    unchanged.
13. **The minus signs were inconsistent.** Computed values used an ASCII hyphen
    (`-2.828`) while `−∞` used U+2212. → screen output is U+2212 throughout, with
    a separate `fmtInput` giving ASCII to `<input type="number">`; a U+2212 there
    makes the browser blank the field. Unit tests pin both rules.
14. **An uppercase transform on table headers changed lowercase symbols.** The
    `q` and `k` in the dot-product table showed as `Q` and `K`, disagreeing with
    the labels above → transform removed.
15. **The exp values in the softmax lab looked different from the prose.** The
    prose has $e^4 \approx 54.598$; the lab shows the stabilised `1.000`. The
    probabilities are the same, but a reader comparing the two is confused →
    a note directly under the lab explains why they differ and that the
    probabilities agree, with a link to that section.
16. **The summary line in the dot-product lab read "q · k = q · k"** → a
    `showSentence` prop on `TermExpansion` turns the sentence off when it would
    repeat itself.
17. **The rewrite pass removed wording forbidden by section 7 of the plan** —
    "평균이 나오는 게 당연하죠", "그냥 크기 비교용 숫자", "그냥 더합니다",
    "simply added" in the English, and so on.

## 5. Checks that were not run

The following are **not recorded as passing.**

- **An actual GitHub Pages deployment** — the repository has no git remote, so
  the owner and the domain could not be confirmed. `astro.config.mjs` has no
  `site`, and no sitemap is generated. What was verified is the behaviour of a
  build made with `base: '/llm-visual-lab/'` served at that same path. Writing
  the workflow and configuring Pages remain to be done.
- **A real screen reader** — labels, `aria-*`, `<desc>` and the non-colour cues
  (−∞, the × marks) were checked at the markup level, and there is no automated
  check. Nobody has actually listened to it with NVDA or VoiceOver.
- **Real devices** — only Playwright's viewport emulation. Touch and performance
  on a real device are unverified.
- **Firefox and Safari** — Chromium only.
- **Reading it with Korean readers** — whether the prose actually lands is
  something only a person reading it can tell you.
- **Automated accessibility tooling (axe and the like)** — not wired up.

## 6. Deliberately not built in this scope

Everything from P2 onward in section 12 of `docs/plan.md`. On screen these appear
in the contents with their stage marked and **no link**.

- Multi-head attention, the output projection W^O, sinusoidal positional
  encoding (P2)
- FFN, residual connections, layer normalization (P2)
- Training, the loss, derivatives, generation and beam search (P3)
- The results tables and the ablations (P4)
- The wiki beyond the paper, graph navigation, search, 3D, shared state,
  autoplay, baseline comparison

## 7. Open problems

1. **Lab state is not preserved across a language switch.** Section 10 of
   `docs/plan.md` asks for "the same document and the same lab state when the
   language changes"; today the document is preserved and the lab returns to its
   starting values. It is not implemented, so it is not recorded as passing.
   Storing it in sessionStorage under a lab id would be the simplest route.
2. **On mobile the contents dropped below the article.** The "contents" link in
   the masthead gets you there, but a collapsible menu would be better.
3. **The review status in `docs/papers/...coverage.md` is maintained by hand.**
   It will drift as the documents grow.
4. **The Project section of AGENTS.md is empty.** Filling in the commands and
   conventions would help, but editing an instruction file needs an explicit ask,
   so it was left alone. Section 1 of this document stands in for it.

## 8. Next

Section **P2 — the components and the whole forward pass** of `docs/plan.md`.
Suggested order:

1. **Multi-head attention** — add a head dimension to the existing `AttentionLab`.
   Running `computeAttention` per head and then concatenating and applying W^O
   keeps the structure of the calculation functions as it is. This closes the
   first item of "What this example does not show".
2. **Positional encoding** — visualising the sin/cos waveforms, plus a
   `math/trigonometry` document. It already has its place in the contents.
3. **FFN, residual connections, normalization** — needs a mean/variance
   visualisation.

Each of these updates the corresponding row of
`docs/papers/attention-is-all-you-need-coverage.md`. When a new calculation is
added, putting the independent reference into
`tests/fixtures/generate_fixtures.py` first keeps the implementation and its
check from sharing a mistake.

---

# Design rebuild (2026-09-11)

The scope and the reasons are in `docs/current-task.md`. In short: the content
and the calculations met their goals but the visual work did not, and the causes
were a palette chosen out of habit, default typefaces, and the prose and the labs
being made of the same material.

## What changed

### The tokens were rebuilt in three layers

`src/styles/tokens.css` was rewritten completely: primitives (ramps, palettes,
scales) → semantics (surfaces, ink, lines, data) → component values (cell sizes,
grid rules).

The dark theme became **one line per token** through `light-dark()`. It used to
be 39 declarations copied by hand into a second block, so fixing one side left
the other behind. All three states (OS setting / forced dark / forced light) were
checked in Chromium before this was adopted.

### The colours were measured, not chosen

The validator from the `dataviz` skill was run against the actual surface colours.

| Role | Light | Dark |
| --- | --- | --- |
| Q | `#2a78d6` | `#3987e5` |
| K | `#eb6834` | `#d95926` |
| V | `#1baf7a` | `#199e70` |
| heatmap | the blue sequential ramp `--seq-0…7` | the same ramp, restepped for dark |

All-pairs passes everywhere. Worst CVD separation ΔE 9.2 (light) / 9.4 (dark).

**Where the measurement changed the design** — in light mode K came out at
2.99:1 and V at 2.82:1 against the surface, below 3:1. So Q, K and V are **never
used as text colour.** A colour chip before the name, the row and column
highlight bands, and a 2px underline under the term numbers carry the identity
instead; the text is ink. This matches the dataviz rule, "text never wears the
data color".

### Typefaces

Pretendard variable (dynamic subset) plus JetBrains Mono, both OFL and both
self-hosted. Every serif was removed: a Latin serif heading over Korean
sans-serif body text reads as an accident rather than as a pairing.

### Two materials

- **The reading surface** — neutral near-white, 17px body on 1.75 leading,
  hierarchy made from weight. The current-document mark in the contents changed
  from a filled pill to a rule down the left.
- **The working surface** — a panel colour distinct from the prose. Shadows
  removed, radii reduced from 8px to 5px. Matrices are a joined grid through
  `border-collapse`, not a set of separate boxes. Every number is monospaced.

### Interaction graphics

1. **Connectors** — selecting a result cell draws real SVG curves from that row
   of the left matrix and that column of the right one. `ConnectorOverlay.tsx`
   measures the DOM for the coordinates. The lines pass **behind** the matrices;
   in front they cut across the middle matrix and looked like a drawing error.
   They work in the matrix-product lab and at the projection, scores and
   weighted-sum steps of the attention lab.
2. **The step rail** — seven pills became a rail that shows the pipeline. A line
   runs behind the list and each numbered circle punches through it with its own
   background.
3. **Formula linkage** — only the term for the current step comes forward; the
   rest step back. Q, K and V are distinguished by underline.
4. **Motion and the heatmap** — transitions on the bars and the connectors,
   respecting `prefers-reduced-motion`. The heatmap moved to the validated
   sequential ramp.

## Real bugs fixed along the way

These were behaviour, not design, but the same files were open.

| Where | Problem |
| --- | --- |
| `lab.css`, heat path | `--heat-ink` did not apply on the non-selectable path, leaving poor text contrast on the dark cells of the `output` step |
| `lab.css`, heat vs row highlight | The heatmap overwrote the row-highlight background and the highlight vanished. Heat cells now show it as a ring |
| `MatrixGrid.tsx` | A blocked cell also got `is-heat`, so `--blocked-bg` never applied (the softmax step with the mask on) |
| `AttentionLab.tsx` | `.formula__sub` had no CSS, so the d in `d_k` never dropped to a subscript |
| `DocLayout.astro` | `.pager__link--prev` had no rule |
| `DocLayout.astro` | `.toc__link--h2` had no rule and rendered unstyled on every document |
| `PathLengthDiagram.astro` | An inline stroke attribute lost to the `.tmap__line` class, so the emphasis on token 1 never showed |
| After renaming tokens | Three SVG components still referenced tokens that no longer existed. A full sweep found and fixed 25 of them |

That last one is a regression the rebuild caused. Renaming a token silently
breaks inline `var()` references outside the CSS. A script that sweeps for
undefined `var()` references caught it, and there are now none.

## Verification

| | Result |
| --- | --- |
| Maths | **pass** — 99 vitest cases. The calculations were untouched and nothing regressed |
| Browser | **pass** — 92 Playwright cases (46 desktop + 46 mobile) |
| The class-name contract | **pass** — not one class name used as a test selector was changed. Only the contents of the CSS rules were replaced |
| Colours | **pass** — all-pairs revalidated against the real surface colours, light and dark |
| Undefined tokens | **pass** — zero, by a full `var()` sweep |
| Types | **pass** |
| Screens | 44 re-shot and looked at (light and dark × desktop and mobile) |
| Sideways scroll | **pass** — 390 / 768 / 1440 / 200% zoom |
| Update time | **pass** — 25.5ms at worst (the limit is 100ms) |

One WARN remains: V is at 2.82:1 against the light surface, below 3:1. The
mitigation the validator asks for — visible labels or a table view — is already
satisfied: V is never used as text colour, and every matrix shows its numbers.

## Checks that were not run

- **`light-dark()` in Firefox and Safari** — Chromium only. In a browser without
  support the light theme shows. Reverting means going back to the two-block form.
- **Layout shift while the fonts load (CLS)** was not measured. The dynamic
  subset made it imperceptible, but imperceptible is not measured.
- Real devices, screen readers and automated accessibility tooling: as before,
  not done.

## Open problems

1. **Connectors exist only at some steps of the attention lab.** They work at
   projection, scores and the weighted sum; divide, mask and probabilities are
   not of the form A·B=C, so they have none.
2. **Lab state is not preserved across a language switch.** Carried over.
3. **Browser support for `light-dark()`** is still on the unverified list above.

---

# Single-page rebuild and a full rewrite of the prose (2026-09-11)

## Why

Two things were raised.

1. **Structure** — the content was split across eight documents and listed as
   fragments in the sidebar. It should be one menu item, one page, divided into
   sections.
2. **The prose** — stop leading with "the paper" and rewrite it as something a
   beginner learns theory from. Use many more examples.

## What changed

### Structure

Routes dropped from **17 to 5**.

| Path | Contents |
| --- | --- |
| `/` | root redirect |
| `/{ko,en}/` | a short landing page |
| `/{ko,en}/attention-is-all-you-need/` | the whole article, twelve sections *(moved to `/{ko,en}/` later on 2026-09-11; see the last section)* |

- The sidebar became the contents of the article. The right-hand TOC and the
  previous/next pager were removed (no test referenced them, so it was safe).
- **Section anchor ids are fixed.** Slugging them from the heading text breaks
  every link whenever a heading is reworded. `src/components/site/Section.astro`
  takes an `id`, and `SECTIONS` in `src/lib/nav.ts` is the single source.
- Scroll position was added. An `IntersectionObserver` marks the section being
  read in the sidebar. Without JavaScript the mark is absent and the links still
  work.
- The six sections not written yet hold **their place only**, marked "not written
  yet" with their stage. They are not links in the sidebar.

### The sections

| # | id | Title | Status |
| --- | --- | --- | --- |
| 1 | `problem` | Turning one sentence into another | written |
| 2 | `big-picture` | The whole picture first | written |
| 3 | `numbers` | Handling many numbers at once | written |
| 4 | `similarity` | Measuring how well two things match | written |
| 5 | `proportion` | Turning scores into proportions | written |
| 6 | `attention` | Computing it yourself | written |
| 7–12 | `multi-head` … `results` | Splitting into several views … How well it did | placeholder |

The mathematics is not a separate menu: it is **folded into the flow** as
sections 3, 4 and 5.

### The prose

**The frame changed.** What used to open with "what this paper claimed" now opens
with "The animal didn't cross the street because **it** was too tired — what is
'it' here?". The correspondence with the paper was taken off the reader-facing
page and moved to one line at the very end plus the coverage document.

**Duplication that was merged** — found by exploring the text.

| Topic | Before | After |
| --- | --- | --- |
| √d_k and how spread out the scores get | 3 places | once, at the end of `#proportion` |
| Why the mask is added before the softmax | 2 places | once, in `#attention` |
| "values chosen by hand" | 3 places | once, in `#numbers` |
| Transpose | 2 places | once, in `#numbers` |
| `## Next` hand-off paragraphs | 7 places | all removed — one page needs no hand-off |

**A real problem fixed** — the actual numbers of `W_Q`, `W_K` and `W_V` appeared
nowhere in the prose, so a reader could not check `Q = XW_Q` on paper. All three
matrices are now printed in `#numbers`. The example in `#attention` was also made
to import from `examples.ts` (some of it had been hard-coded).

## Verification

| | Result |
| --- | --- |
| Maths | **pass** — 99 vitest cases. The calculations were untouched and nothing regressed |
| Browser | **pass** — **86** Playwright cases (43 desktop + 43 mobile) |
| Build | **pass** — 5 routes |
| Types | **pass** |
| Section structure | **pass** — twelve present and in order, the six planned ones marked, Korean and English identical |
| Anchors | **pass** — all six sidebar anchors resolve to real ids |
| Scroll mark | **pass** — `aria-current` follows the scroll |
| Connectors | **pass** — two lines on a wide screen, suppressed on a narrow one |
| Without JavaScript | **pass** — the twelve sections and the default lab values are in the static HTML |
| Responsive | **pass** — 390 / 768 / 1440 / 200% zoom |
| Update time | **pass** — 32.0ms at worst (the limit is 100ms) |

### The tests were rewritten outright

Two things could not be fixed by changing URLs.

1. **`main h1` assertions** — there is only one `<h1>` per page now, so the
   checks had to look at the sections and the sidebar rather than at a document
   title.
2. **Selector collisions** — with four labs in one DOM, selectors like
   `.mgrid--score`, `.terms__result` and `처음 값으로 되돌리기` pick up a
   different lab. (All four components render that label.) Everything was scoped
   under `.lab--*`.

One connector check failed on mobile at first, and **it was the site that was
right**: on a narrow screen the matrices stack and the lines are suppressed
deliberately. The check was split by viewport so both behaviours are asserted.

## Files deleted

Twelve documents were removed; their content moved into the new single page.

```
src/content/docs/{ko,en}/papers/attention-is-all-you-need.mdx
src/content/docs/{ko,en}/papers/attention-is-all-you-need/{background,architecture,attention}.mdx
src/content/docs/{ko,en}/math/{vectors-and-matrices,dot-product,softmax}.mdx
```

**The repository has no commits at all**, so the sixteen originals were copied to
the scratchpad before anything was deleted. Making one commit would be safer.

## Where this departs from the plan

It does not follow the information structure of section 3 of `docs/plan.md`
(`papers/`, `math/`, `concepts/` as separate documents) or the reader-facing
citation style of section 4.1. At the user's request the content was merged into
**sections within one page** and the paper's section numbering was taken off the
reader-facing screen. The correspondence itself is still maintained in
`docs/papers/attention-is-all-you-need-coverage.md`.

## Open problems

1. **Four React islands on one page**, all `client:load`, so they all hydrate on
   first load. Deferring to `client:visible` would lighten it but could make the
   tests flaky, so it was left as it is.
2. **Connectors exist only at steps of the form A · B = C.** Divide, mask and
   probabilities have none.
3. `light-dark()` verified in Chromium only (carried over).
4. Lab state is not preserved across a language switch (carried over).

---

# The two rails rearranged, and more diagrams (2026-09-11)

The scope is in `docs/current-task.md`.

## Why

On the landing screen the left sidebar (the twelve sections of the article) and
the landing page itself ("what you can read now: one link") were talking about
different things. What was decided: remove the landing page, and split the two
levels — **the left rail is the whole learning path, the right rail is the
contents of the article being read.**

## What changed

### The article became the home page

`src/content/docs/{ko,en}/attention-is-all-you-need.mdx` → `index.mdx` in the
same directory. The two old landing pages were deleted and their "How to read it"
guidance moved into the opening of the article. `parseEntryId` strips the
trailing `index`, so no routing code changed.

Routes dropped from **5 to 3**: the root redirect, `/ko/` and `/en/`.

### Left rail = the learning path (`src/lib/curriculum.ts`)

| Group | Entries |
| --- | --- |
| Where it starts | Attention Is All You Need — the only document that can be read |
| Where it goes (later) | Autoregressive language models / Scale and generalisation / The modern decoder block / Long context and inference cost / Sparse structure / Post-training and reasoning / Multimodal input and tools / Other sequence structures / Anatomy of a recent model |

These are the expansion directions from section 6 of `docs/plan.md`, carried
over. Planned entries are text, not links. The "later" badge sits beside the
group heading once — per item it repeats nine times and pulls the eye there.

### Right rail = the contents (`src/components/site/PageToc.astro`)

The twelve sections are rendered on the server, and the sub-headings of the
section being read are folded in **underneath that section only**, by script. One
section is open at a time, and the list swaps as you move between them.

`Section.astro` renders the h2, so section headings never reach Astro's
`headings` array while the sub-headings arrive flat — there is no way to group
them at build time. Hence the pass over the DOM at load.

Without JavaScript the twelve-line contents list remains (pinned by a check).

### Layout

| Width | Layout |
| --- | --- |
| 75rem and up | Three columns — path 13rem / article / contents 14rem |
| 60–75rem | Two columns — path / article, contents below the article at article width |
| under 60rem | One column — article → contents → path |

### Five more diagrams

| Component | Section | What it shows |
| --- | --- | --- |
| `AttentionArcs` | `#problem` | Arcs from **it** to animal / tired / street in the opening sentence |
| `TokenPipeline` | `#big-picture` | Sentence → tokens → ids → a row of numbers → ⊕ position |
| `ShapeFlow` | `#numbers` | The inner 4 cancelling in `3×4 · 4×2 = 3×2` |
| `QKVRoles` | `#attention` | One token branching into Q, K and V |
| `AttentionSteps` | `#attention` | The four steps of equation (1) and the shape each produces |

Rules kept:

- Dimensions come from `examples.ts`. No 3, 4 or 2 is written into a figure.
- The step names in `AttentionSteps` come from the same `ui.lab.steps` as the
  lab's step rail, so the figure cannot say "scores" while the rail says
  something else (pinned by a check).
- The thicknesses in `AttentionArcs` are not computed. No number appears in it,
  and the caption and `<desc>` both say the weights are illustrative. The prose
  promises that every number on screen is really calculated, so the distinction
  matters.
- Seven figures share one DOM, so every id prefix differs: `aa-`, `tp-`, `sf-`,
  `qkv-`, `as-`, `pld-`, `tmap-`. `TransformerMap`'s `tmap-title`/`tmap-desc`
  were renamed to the `-t`/`-d` form the others use.

## Real bugs fixed

| Where | Problem |
| --- | --- |
| `base.css`, html and body | `overflow-x: hidden` makes the other axis compute to `auto`, which turned this into a scroll container — and **the sticky left sidebar had never actually been sticking** (it scrolled away with the content). Fixed with `overflow-x: clip`. |
| `base.css`, tables | The prose table rules were scoped to `.prose > table`, but tables live inside `.doc-section`, so **none of them ever applied**. Added `.doc-section > table`. |
| `TokenPipeline`, first draft | Five arrows left the ids but only one block received them, so four pointed at empty space. Redrawn with the tokens stacked vertically, one row each. |
| `AttentionSteps`, first draft | The "shape that comes out" label overlapped the first box's dimensions. A left gutter was added and a Q/K input box put there. |

## A mistake I made, and the recovery

**A block of CSS rules was deleted by accident.** While replacing the styles for
the right-hand rail, a Python replacement used the `/* Sections */` comment as
its end marker — and `.prose`, `.doc-header*`, `.badge*`, `.note*`, `.expand*`,
`.btn*`, `.segmented`, `.site-footer*`, `.figure*` and `.tmap__*` all sat
between the two markers. About 390 lines disappeared.

**All 94 Playwright checks still passed**, because the text still read and the
buttons still worked. It was only visible on looking at a mobile screenshot: the
status badge and the aside boxes were rendering as bare text.

With no commit in the repository there was nothing to revert to. The rules were
rewritten from the surviving token system and from screenshots taken just before
the mistake (`lab-attn-1440.png`, `top-desktop-light.png`), and the result was
compared against them side by side.

**Checks were added so it cannot happen silently again** (three, under "the
styling is still attached"):

- Sixteen key parts are not left at their unstyled defaults (is `.prose` a grid,
  does `.note` have a left border, does `.badge__dot` have a background colour, …)
- The chosen and unchosen sides of a segmented control look different
- Every CSS variable referenced is defined in `:root` (excluding the ones
  components set inline)

## Verification

| | Result |
| --- | --- |
| Maths | `npx vitest run` — **99 passing**. The calculation code was untouched |
| Types | `npx tsc --noEmit` — clean |
| Build | `npx astro build` — **3 routes** (root redirect + `/ko/` + `/en/`) |
| Browser | `npx playwright test` — **100 passing** (50 desktop at 1440px + 50 mobile at 390px) |
| Links | The only internal targets are `/llm-visual-lab/ko/` and `/llm-visual-lab/en/`, both 200 |
| Right rail | Moving to `#numbers` swaps in that section's six h3s, checked. The sub-heading anchors resolve, percent-encoded Hangul slugs included |
| Without JavaScript | The twelve-line contents (6 links + 6 planned) remains |
| Diagrams | All seven render, with `<title>` and `<desc>` over 60 characters, each in the right section, none overflowing its frame |
| Responsive | 390 / 768 / 1280 / 1440 / 200% zoom — no sideways scroll |
| Screens | 32 re-shot, light and dark × desktop and mobile, looked at |

### Browser checks added

- Left rail: only readable documents are links, and exactly one carries
  `aria-current="page"`
- Right rail: one section open at a time / swaps as sections change / the open
  sub-headings match that section's h3s
- Layout: at 1440px the path is left of the article and the contents right of it;
  at 1100px the contents drops below
- Diagrams: seven render, with alt text, in the right section, inside the frame
- The dimensions in the figures match `examples.ts`, and the step names match the
  lab rail
- The three styling-regression checks above

## Checks that were not run

- **Firefox and Safari.** Chromium only. `overflow-x: clip` works from Safari 16,
  but that was not confirmed directly.
- **Real devices.** Mobile is 390px viewport emulation.
- **Assistive technology.** Nobody has listened to it. The right rail is swapped
  in by script, so how it is announced needs checking.
- **Re-running the colour validation.** No colour token changed in this scope, so
  the `dataviz` check was not repeated.

## Open problems

- While a planned section (7–12) is on screen, the current mark in the contents
  stays on section 6, because planned sections are not links and are therefore
  not observed. It is not jarring, but it is not accurate either.
- Earlier sections of this log still contain the old
  `/{ko,en}/attention-is-all-you-need/` path. They are a record of that moment
  and were left alone; the current paths are the ones in this section.
- **The repository still has no commits at all.** Files were deleted in this
  scope and a block of CSS was lost and rewritten. A commit first would be wise.

### Last adjustments, made after looking at the screens

- The title was fixed at 44px and took three lines on a 390px screen. It is now
  `clamp(--fs-800, 7.5vw, --fs-1000)`, giving 29px over two lines. Section
  headings were tightened the same way. The rewritten CSS had contained no media
  queries at all, which is how this arose.
- The prose tables (`크기 정리`, `예제의 크기`) **had been unstyled ever since the
  single-page merge**: the rule was `.prose > table` while the tables live inside
  `.doc-section`. They render properly for the first time now. The
  `text-transform: uppercase` on `th` was confirmed not to touch the equations
  (KaTeX inside `td` computes to `none`).
- On mobile the contents sits below the article rather than to the right, so the
  guidance text no longer asserts a direction.
- Added `scroll-margin-top` so the sticky masthead does not cover the contents
  when the masthead's "contents" button jumps to it.

## Next

- P2 — the actual content of `#multi-head`, `#positions` and `#blocks`
- Decide whether the current mark in the contents should follow into the planned
  sections

---

# Internal documents and comments moved to English (2026-09-11)

A standing convention for the repository: internal documents and code comments
are written in English, and only the Korean wiki page is in Korean.

## What changed

| | |
| --- | --- |
| `docs/` | `progress.md`, `current-task.md` and the coverage map are now in English. `docs/plan.md` stays in Korean and was added to `.gitignore` — it is the author's own design brief, not a project file |
| Comments | Every `//`, `/* */` and JSDoc comment in `src/`, `tests/` and `scripts/`, including the CSS (171 lines of it) |
| Tests | Test names and assertion messages in `site.spec.ts` and `math.test.ts`. Assertions that match on-screen text keep their Korean, because that is what the Korean page renders |
| Fixtures | The case names in `generate_fixtures.py` were translated and `reference.json` regenerated. Verified that, names and comments aside, the numbers are byte-for-byte identical |
| Untouched | `ui.ts` (the Korean locale object), `title.ko` in `nav.ts` and `curriculum.ts`, `labText.ts`, the `L` label objects in the diagram components, the SVG `desc` alt text, and all of `src/content/docs/ko/index.mdx` |

## Error messages moved into the i18n layer

The maths threw Korean messages, and they are **shown to the reader**: the
attention lab catches them and prints `e.message` in its error panel. Translating
them to English outright would have left the Korean page with an English error
under a Korean heading.

- `src/lib/math/errors.ts` — new. `CalcError` carries a `code` and the numbers
  that explain it; `ShapeError` and `MaskError` extend it. The `message` on the
  Error itself is English, for logs and for callers with no locale to hand.
- `src/lib/i18n/calcError.ts` — new. Turns a `CalcError` into a sentence in the
  reader's language, falling back to the raw message for anything else.
- `ui.ts` gained `lab.errors` (one sentence per code) and `lab.matrixNames`. The
  English locale reuses `EN_CALC_ERRORS` directly, so the wording exists once.
- `assertRectangular(m, name)` now takes a `MatrixKey` (`'left'`, `'X'`, `'W_Q'`,
  …) rather than a display string, so no Korean remains in the maths.

Unit assertions moved from matching prose to matching `code`, which is what they
were actually trying to pin. Five checks were added: every code has a sentence in
both languages, the reader gets their own language, the matrix is named in that
language too, no placeholder is left unfilled in either language, and anything
that is not a `CalcError` falls back to its raw message.

The last Korean literal in a component, `` `토큰 ${i + 1}` `` in `AttentionLab`,
moved to `ui.lab.tokenLabel` as well. It had duplicated `ui.lab.token` and could
have drifted from it.

## Verification

| | Result |
| --- | --- |
| Maths | `npx vitest run` — **104 passing** (99 + 5 new) |
| Types | `npx tsc --noEmit` — clean |
| Build | `npx astro build` — 3 routes |
| Browser | `npx playwright test` — **100 passing** |
| Fixtures | `reference.json` regenerated; with names and comments stripped, the JSON is identical to before |
| No Korean left where it should not be | Swept `src/`, `tests/` and `scripts/`: what remains is only the Korean locale strings, the diagram labels, the `ko` MDX, and the on-screen text the tests match against |

## Open problems

- `docs/plan.md` is gitignored, so a fresh clone will not have it. Comments in
  `examples.ts`, `math.test.ts` and `generate_fixtures.py` cite its section
  numbers, and those references will dangle for anyone else.
- `EditableMatrix.tsx` builds its `aria-label` fallbacks as the English `row N` /
  `column N` regardless of locale. Pre-existing, and out of scope here.

---

# One content width for the whole article (2026-09-11)

## Why

The prose and the figures had different right edges: body text was held to a
38rem measure while figures and labs broke out to the full column. Side by side
that reads as an accident rather than as a choice.

Measured, at a 1440px viewport: paragraphs 608px, figures and labs 840px — a
232px difference.

## What was decided

Match everything to **the wider side**, as asked. The cost was measured first and
accepted: characters per line go from 54 to 76 in Korean, and from 77 to **109**
in English. The English figure is well past the usual 45–75 guideline; it was
put to the user with those numbers and the wider edge was chosen anyway.

Narrowing instead was the alternative, and it was rejected on evidence: the
seven-step rail in the attention lab already starts scrolling inside itself once
the column drops to about 712px, so the labs have very little room to give.

## What changed

| | |
| --- | --- |
| `src/styles/base.css` | `.prose` and `.doc-section` are single-column grids now. They stay grids — margins do not collapse in a grid and the vertical rhythm is tuned on that — but there is no narrow column to break out of, so the four `grid-column: 1 / -1` breakout blocks are gone |
| `src/styles/base.css` | `.main` takes `max-width: var(--main-max)` and `margin-inline: auto` |
| `src/styles/base.css` | The independent `max-width: var(--measure)` on `.doc-header__lead` and `.figure__caption` removed — they would otherwise have stayed narrow while everything around them widened |
| `src/styles/lab.css` | Same for `.lab__note`, `.lab__what` and `.lab__guide .note`, which had the identical mismatch inside the lab panel. `.lab`'s own `max-width: var(--measure-wide)` was dead code — the column never reached 58rem — and is gone |
| `src/styles/tokens.css` | `--measure` and `--measure-wide` replaced by a single `--main-max: 52.5rem` |

52.5rem is what the middle column already works out to in the three-column
layout. The cap only bites on narrower windows: below 60rem the rails drop away
and the column would otherwise reach about 920px, giving the **longest** lines on
the **smallest** window. With the cap the width is monotonic and never exceeds
840px.

Two dead `grid-column: 1 / -1` declarations (`.prose hr`, `.doc-header`) were
removed at the same time; in a one-column grid they say nothing, and they imply a
second column that no longer exists. The `astro-island > .lab` selectors went
with the breakout blocks — they had never matched anything, because Astro gives
the island `display: contents` and the `.lab` itself is the grid item.

## Verification

| | Result |
| --- | --- |
| Widths | Measured at 1680 / 1440 / 1280 / 1100 / 900 / 768 / 390, in both languages: paragraph, note, lead, caption, figure frame, table and lab all come out identical. The only inset is the lab's own panel padding |
| The cap | main never exceeds 840px, including just below the 60rem breakpoint where it used to reach 920 |
| Maths | `npx vitest run` — 104 passing |
| Types | `npx tsc --noEmit` — clean |
| Browser | `npx playwright test` — **108 passing** (100 + 8 new) |
| Sideways scroll | 390 / 768 / 1280 / 1440 / 200% zoom, unchanged |
| Screens | 32 re-shot, light and dark × desktop and mobile, looked at |

### Checks added

- `the article has a single right edge at {1440,1280,900}px` — seven elements
  compared for equality. Confirmed it fails on the old layout: restoring the
  two-column grid in the browser gives 608 / 840 / 840.
- `the column never grows past its cap, however the rails fall away` — checks
  1440, 1100, 959 and 900, the last two being the case the cap exists for.

## Open problems

- The diagrams are drawn at fixed widths (`ShapeFlow` 360, `TokenPipeline` 540,
  `AttentionArcs` 644) and centred in an 840px frame, so several of them now sit
  in a lot of empty space. Stretching every SVG to fill would coarsen the ones
  deliberately drawn small; leaving them centred is the lesser problem, but the
  frames could be sized to their content instead.
- English at 109 characters per line is long by any typographic guideline. It was
  a deliberate choice, recorded here so it is not mistaken for an oversight.

---

# The remaining six sections, and more diagrams (2026-09-12)

Sections 7 to 12 held their place in the outline and nothing else. They are written
now, and twelve more diagrams went in with them.

## The calculations came first

Everything the prose quotes is computed on the page, so the maths had to exist
before the writing did. Five new modules, each with an independent reference
generated a different way in `tests/fixtures/generate_fixtures.py`:

| Module | What it computes |
| --- | --- |
| `multihead.ts` | Runs `computeAttention` once per head, joins the outputs side by side, applies W_O. Reusing the single-head function is deliberate — a head cannot quietly disagree with the lab |
| `positional.ts` | The sinusoids of §3.5, keeping the divisor, the angle and the wavelength per term, not only the value |
| `blocks.ts` | The residual path, layer normalization per row, and equation (2) with its ReLU |
| `training.ts` | Cross-entropy against any target, label smoothing, and the schedule of equation (3) |
| `generation.ts` | Beam search over a hand-written toy model. Width 1 is greedy, which is how the article shows both with one function |

The fixture generator computes the same things by a different route — numpy for the
linear algebra, the definitions written out for the rest — and the regenerated
`reference.json` left every earlier value byte-for-byte unchanged.

## The paper's own numbers

`src/lib/paper.ts` is new. Tables 1, 2, 3 and 4 and the training conditions, read
out of the PDF with `pdftotext` and transcribed once.

These are **the only numbers on the site that were not computed here**, and they are
kept apart for that reason. A unit test pins every figure the prose quotes, so the
transcription cannot drift from the article.

The BLEU-against-cost scatter reads that module directly rather than repeating the
numbers.

## What was written

| Section | What it covers | Diagrams |
| --- | --- | --- |
| `#multi-head` | Why one distribution cannot watch two things; a second head added to the worked example; joining and W_O; why more heads is not more arithmetic | `MultiHeadSplit`, `HeadPatterns` |
| `#positions` | What the numbers have to satisfy; sine and cosine; why the pairs run at different speeds; why added rather than concatenated | `PositionWaves`, `PositionGrid` |
| `#blocks` | Feed forward as widen-cut-narrow; the residual bypass; normalization; a whole layer written out | `FeedForwardShape`, `ResidualPath`, `LayerNormSteps` |
| `#training` | The loss as one number; label smoothing; which way to nudge; the learning-rate schedule; the conditions the paper used | `LossPicture`, `LearningRateCurve` |
| `#generation` | Why training and generation are different shapes; greedy losing to beam search on a worked tree; the paper's settings | `TrainAndGenerate`, `BeamTree` |
| `#results` | What BLEU is; score against cost; which ablations mattered; parsing; the limits the paper names itself | `BleuCost` |

Twelve new diagrams, nineteen in total. Every one of them reads its numbers from
`examples.ts`, from the maths modules, or from `paper.ts` — none has a value typed
into it by hand.

`HeadPatterns` is the clearest case: it draws the two heads' **computed** attention
weights, and the point it makes only lands because those two grids really do differ.
A check asserts that they do.

## Things stated as not covered

The article says these plainly rather than passing over them:

- how the gradient is actually computed (backpropagation needs calculus, and the
  article promised arithmetic)
- what dropout does arithmetically — named, with the paper's measured effect
- the exact definition of BLEU
- sampling as an alternative to beam search, and caching during generation
- the shared embedding weights and the √d_model factor of §3.4

The coverage map records each of these against the section it belongs to.

## Verification

| | Result |
| --- | --- |
| Maths | `npx vitest run` — **147 passing** (104 + 43 new) |
| Types | `npx tsc --noEmit` — clean |
| Build | `npx astro build` — 3 routes |
| Browser | `npx playwright test` — **112 passing** (108 + 4 new) |
| Fixtures | Regenerated; every pre-existing value identical, seven new blocks added |
| Diagrams | All 19 render in both languages, each in its own section, each with alt text over 60 characters, none overflowing its frame |
| Screens | 56 re-shot (12 sections × 2 widths × 2 themes, plus both articles), looked at |

### Checks added

- Every new calculation against its independent reference: multi-head per head and
  after W_O, the sinusoids, layer norm, the feed-forward network, the loss with and
  without smoothing, the schedule, and beam search at several widths.
- Properties, not just values: one head with an identity W_O equals plain attention;
  a normalized row has mean 0 and variance 1; normalizing is unaffected by a constant
  shift; the feed-forward network treats each position alone; a wider beam never
  finds a worse sentence; smoothing raises the loss.
- `no section is a stub` — every section must have real length, more than one
  sub-heading, and something to look at. This is what would catch a placeholder
  dressed up as a finished section.
- The paper's transcribed numbers, including the claim the prose makes from them
  (the base model beats every earlier row while costing less).

## Open problems

- **Multi-head has no lab.** The single-head attention lab is interactive; the
  multi-head section is figures and prose only. Adding a head dimension to
  `AttentionLab` is the natural next step and was left out of this scope.
- **`#training` explains the gradient without computing one.** That is stated in the
  section, but it is the one place where a reader who wants to follow every number
  cannot.
- The diagrams are still drawn at fixed widths and centred in a wider frame, so
  several sit in empty space. Unchanged from the previous scope.

---

# The learning path lists only what exists (2026-09-15)

## Why

The left rail's "이어지는 길 / Where it goes" group was badged "예정 / later" and listed
nine follow-up topics. Asked whether that was right: it was not. The titles came from
section 6 of `docs/plan.md`, which calls them recommended directions and explicitly
not a sequence; P5 scopes one change document at a time; and the plan says being
current is not solved by fixing model names in advance. None of the nine had a scope,
an order or a date. Presenting them as a scheduled path — and reading "a later stage
covers it" to screen readers — promised work that did not exist.

## What changed

| | |
| --- | --- |
| `src/lib/curriculum.ts` | The nine items removed. `CourseItem.status` and `CourseGroup.planned` removed: the menu now holds readable documents only, and an empty group means "in preparation" |
| `src/components/site/Sidebar.astro` | An empty group shows a chip beside its heading; the planned-item branch is gone |
| `src/lib/i18n/ui.ts` | `nav.later` ('예정' / 'later') → `nav.inPreparation` ('준비중' / 'in preparation') |
| `src/styles/base.css` | The unused `.sidebar__pending` rule removed. `.sidebar__flag` is now `inline-block` + `nowrap` |
| `src/content/docs/{ko,en}/index.mdx` | "How to read it" no longer calls the menu "the whole road this wiki will cover", and the closing note no longer says the post-2017 material "has its place" in it |

### A bug found while looking at it

The English chip, "in preparation", is long enough that it split across two lines
inside its own border ("in" / "preparation") in the 208px rail. It now wraps as a
single unit. The markup also gained a real space between the heading and the chip,
because without one the accessible text read "이어지는 길준비중" and
"WHERE IT GOESin preparation" as single words.

## Verification

| | Result |
| --- | --- |
| Types | `npx tsc --noEmit` — clean |
| Build | `npx astro build` — 3 routes |
| Maths | `npx vitest run` — 147 passing (untouched) |
| Browser | `npx playwright test` — **112 passing** |
| Rendered menu | ko: "출발점 · Attention Is All You Need · 이어지는 길 준비중"; en: "Where it starts · Attention Is All You Need · Where it goes in preparation" |
| Screens | the menu shot in ko/en × light/dark × 1440/390 and looked at |
| Leftovers | no `sidebar__pending`, no `nav.later`, none of the nine titles anywhere in `src/` or `scripts/` |

The rewritten check asserts the chip text in both languages, that the removed titles
and the word "예정" do not appear, that the English chip occupies one line, and that a
space separates it from the heading. The one-line assertion was confirmed to catch the
bug: restoring the old inline style in the browser gives two line boxes.

## Open problems

- **The architecture diagram in `#big-picture` is now out of date in the same way.**
  `TransformerMap` still fades Add & Norm, Feed Forward, Linear and Softmax, and its
  caption says the faded blocks are "named here and left for later" (ko: "이름만 알아
  두고 넘어갑니다"). Since the remaining sections were written, `#blocks` works through
  the feed-forward network and normalization with real numbers, and `#training` covers
  the output distribution. Not changed here — outside this request.

# No seams, and a picture at every key point (2026-09-15)

## Why

The reader could not picture "토큰 수만큼의 줄이 쌓인 덩어리가 encoder 로 들어갑니다" in
`#big-picture`. The words for a token's numbers drifted (뭉치 → 한 줄 in a caption → 줄이
쌓인 덩어리) with no sentence making the switch, and no figure showed rows stacking into a
block or the block keeping its row count through the encoder: every figure on the page
was a still, and only four islands existed, all in sections 3–6. The standard restated
for this scope was comprehension above everything — example, moving figure where a still
fails, an interaction at each key point, no ambiguity at any boundary — which is what
`docs/plan.md` §5.3–5.4 and §7 already ask for.

## What changed

- **`BigPictureWalk`** (`src/components/lab/`): the route in seven steppable frames with a
  mini map that lights the current part. `Stepper` was extracted from `AttentionLab` so
  both rails are one component. `TokenPipeline.astro` removed.
- **Prose seams**, both languages: the big-picture section names "한 줄" once, bridges
  rows → block before ⊕, opens step 3 by pointing back at the block, points each step at a
  frame; 덩어리 means one thing; every term the audit found used before its definition
  (갈래, 가리기, 흩어진 정도, BLEU, F1, perplexity, ln, Adam, 투영, Sublayer) is glossed at
  first use; `#proportion` no longer opens on unseen scores; the seven lab steps are mapped
  to the four formula steps; `A⁽²⁾`, `W₁` and `b₁` are printed so every quoted number can
  be checked; the `n × n` cost is stated in `#attention` before `#results` relies on it.
- **Seven explorers** replace seven stills: positions (position + pair), layer norm
  (editable row, add/multiply all, γ, β), residual (bypass on/off over six layers, cos θ
  to the input), loss (p of the right word, ε), beam width (1/2/3), BLEU against cost
  (en-De/en-Fr), head patterns (query token). Copy in `src/lib/i18n/explorers.ts`.
- **Five stills** for claims that had none: `SerialWait`, `TransposeFlip`, `ConcatShape`,
  `HeadBudget`, `BleuExample`.

## Verification

| | Result |
| --- | --- |
| Types | `npx tsc --noEmit` — clean |
| Build | `npm run build` — 3 routes |
| Maths | `npm test` — 147 passing (untouched) |
| Browser | `npx playwright test` — **130 passing** (65 desktop + 65 mobile), including the walk on both pages and one check per explorer |
| Screens | every frame of the walk at 1440 (ko), frame 4 at 390, frame 6 dark; every explorer and new still at 1440 (ko), residual with the bypass off, beam at widths 2 and 3 — all looked at; three drawing bugs found this way and fixed (clipped "× 6", a title over an arrow, unflattened cells) |
| Read-through | 뭉치 and 단락 no longer appear on the Korean page; 덩어리 only means the stacked block |
| Not run | `git status` / `git diff`: git on this machine refuses to run until the Xcode licence is accepted, so the tree was reviewed from the file list instead |

The residual explorer's guidance was rewritten after looking at its numbers: with the
bypass, cos θ to the input stays positive through six layers (0.35 at layer 6); without
it, the direction flips negative from layer 4. The hand-chosen W make the size of the
effect unlike a real model, and the text says so.

## Open problems

- `TransformerMap` still fades the blocks the later sections now cover, and its caption
  still says they are left for later. Carried over from the previous scope.
- `PositionExplorer` and the walk are conceptual in different senses: the walk draws no
  values at all (and says so); the explorer computes real ones for d_model = 4.
- The English `BleuExample` and `SerialWait` copy was written alongside the Korean and
  has not had a separate editing pass.

## The intent before the mechanism (2026-09-16)

A reader stopped at big-picture step 3: "attention — 각 줄이 … 모든 줄을 보고 정보를
섞습니다" says what happens but not why mixing is wanted. A read-through of the Korean
page, the walk copy and the explorer copy found the same shape in about twenty-five
places: the reason usually existed, but landed after the mechanism (sometimes in a
different file, sometimes behind "왜 필요한지는 곧 나옵니다"). A few had no reason anywhere
(Adam, the one-at-a-time ablation, the 4× widening, the order of a layer, sub-word tokens,
three projections at first use).

- **Rule applied**: every mechanism is introduced by the problem it solves in the sentence
  before it. Reasons that already existed later in the same subsection were moved up, not
  duplicated; deferrals were replaced with the one-line reason in place.
- **Where**: `src/content/docs/{ko,en}/index.mdx` (big picture §1–5, three W at first use,
  transpose, score spread, the four steps, Q/K/V, the divide step, the mask, second head,
  concat, sin/cos vs the three conditions, pair speeds, feed forward, residual, layer
  norm, layer order, loss, learning rate, training table + Adam + dropout, BLEU, ablation);
  `src/lib/i18n/labText.ts` (walk frames tokens, row, block, decoder now carry the why in
  `what`); `src/lib/i18n/explorers.ts` (every `note` opens with the question the widget
  answers).

| | Result |
| --- | --- |
| Types | `npx tsc --noEmit` — clean |
| Build | `npm run build` — 3 routes |
| Maths | `npm test` — 147 passing (untouched) |
| Browser | `npm run test:e2e` — 134 passing |
| Screens | walk frames 2, 3 (390), 6 at 1440; heads explorer at 1440; loss explorer at 390 — longer copy wraps cleanly |

Follow-up the same day: walk frames 5 and 6 redrawn. Frame 5 had drawn six rows of
`attention | feed forward` side by side, which read as two parallel columns; the paper's
Figure 1 draws one layer vertically with "N×" beside it. Both frames now draw one layer
large (the block passing attention → feed forward bottom-up, with the block itself drawn
between the parts) and, beside it, the six layers in series as a small chain
(`SixInSeries` in `BigPictureWalk.tsx`), layer 1 highlighted and tied to the big drawing.
The decoder's chain also carries the dashed source line into every layer. The "× 6" label
stays but is no longer what the reader has to count from. Checks re-run: types, build,
147 unit, 134 e2e; frames 5 and 6 screened at 1440/390, light/dark, ko/en.
